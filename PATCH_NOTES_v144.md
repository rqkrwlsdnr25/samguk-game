# 삼국쟁패 v144 지도 패치

두 번째 사용자 스케치를 기준으로 숙신 고토·찰하·연해주 북부를 재구성했습니다. 스케치 색상은 영토 구획 지시로 사용하며 게임의 국가 색상은 유지합니다.

- 숙신 고토를 동녕·옥저 경계까지 확장하고 상단 수로를 흡수합니다.
- 찰하·연해주 북부·숙신 고토는 동일한 점열로 접경을 공유합니다.
- 중앙 진한 파란 호수는 스케치 형태의 수역으로 남깁니다. 호수 위치는 영토 클릭 영역에서도 제외됩니다.
- WORLD.borders의 해당 세 지역 옛 경계선을 실제 공유 변으로 교체합니다.
- GEOGRAPHY.rivers의 옛 호수·수로와 면적 없는 세로선 subpath만 제거합니다.
- 연해주 남부의 삼중 접점과 목단강 유역의 끝점도 맞춰 접경을 정렬합니다.
- 라벨·도시·병력 HUD의 중심은 다각형 면적 중심을 사용합니다.
- 영토 ID와 수를 유지하고 새 접경에 맞춰 육상 이동 연결을 갱신합니다.

## 파일

- EumnuMapDataV144.json: 수정한 좌표·중심·호수 데이터
- EumnuTerritoryPatchV144.js: 동일 데이터를 포함하는 초기화 패치와 선택적 Canvas 렌더러
- index.html: 패치 로드 순서 반영
- map.js: 중앙 호수 표시
- verify-map-v144.cjs: Node 내장 모듈만 사용하는 재현용 검사

기존 전체 소스에 패치가 연결되어 있습니다. JSON은 검토용이며 런타임은 fetch 없이 JS에 포함된 같은 데이터를 읽습니다.

## 검증 범위

JavaScript 문법 검사 및 기존 지도 초기화 코드와 새 패치를 메모리에서 실행했습니다. 173,489개 위치의 표본 검사에서 수정한 세 영토와 주변 영토의 면적 겹침 및 호수 침범은 발견되지 않았습니다. 경계 선분 교차 검사에서도 실제 교차는 발견되지 않았습니다. 세 지역 중심은 각 영토 내부에 있습니다.

브라우저 화면·줌·전투·저장 불러오기 전체 회귀검사는 수행하지 못했습니다. Sites 배포는 완료되지 않았습니다.

## 실행

압축을 풀어 기존 index.html을 열거나 기존 정적 서버로 실행하세요. 최신 패치 파일은 폴더 구조를 유지해야 합니다.

재현 검사:

```sh
node verify-map-v144.cjs
```

Canvas 사용이 필요하면 지도 카메라 변환을 적용한 뒤 호출합니다. 실제 게임은 기존 SVG 렌더링을 유지합니다.

```js
window.SAMGUK_EUMNU_SKETCH_V144.renderCanvas(ctx, {
  zoom: currentZoom,
  fillFor: region => factionColorFor(region.name),
  renderTokens: (ctx, region, x, y) => drawTroopHud(ctx, region.name, x, y)
});
```
